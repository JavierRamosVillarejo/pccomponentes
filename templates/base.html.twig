<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}PCCOMPONENTES{% endblock %}</title>
        {% block stylesheets %}
            <link href="{{ asset('css/estilos.css') }}" type="text/css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
        
        {% endblock %}
    </head>
    <body>
        <div>
        <h1>PC COMPONENTES</h1>
        {%  if app.user %}
            Hola {{ app.user.nombre }} {{ app.user.apellidos }} ({{ app.user.DNI }})
        {%  endif %}
        </div>
        {% block body %}{% endblock %}
    </body>
</html>
